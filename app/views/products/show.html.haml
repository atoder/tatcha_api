%h2.center= @product["name"]
%br

#products
  .box.panel.panel-default
  .center= link_to image_tag(@product['image_url'], height: "200"), @product["url"]
  .panel-body
    .product_name= @product['name']
    .product_description= @product['short_description'].html_safe
